<!DOCTYPE html>
<html lang="vi"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Giao dịch thanh toán</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"></head>
<body class="bg-light"><div class="container-fluid py-4"><div class="d-flex justify-content-between align-items-center mb-3"><div><h1 class="h3 mb-1">Giao dịch thanh toán</h1><p class="text-muted mb-0">Theo dõi COD, MoMo và trạng thái giao dịch.</p></div><a href="{{ route('admin.dashboard') }}" class="btn btn-outline-primary">Dashboard</a></div><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Mã GD</th><th>Đơn hàng</th><th>Khách hàng</th><th>Cổng thanh toán</th><th>Số tiền</th><th>Trạng thái</th><th>Thời gian</th></tr></thead><tbody>@forelse($transactions as $transaction)<tr><td>#{{ $transaction->id }}</td><td>#{{ $transaction->order_id }}</td><td>{{ $transaction->order->user->name ?? $transaction->order->name ?? 'N/A' }}</td><td class="text-uppercase">{{ $transaction->gateway }}</td><td class="fw-bold">{{ number_format($transaction->amount, 0, ',', '.') }}đ</td><td><span class="badge text-bg-{{ $transaction->status === 'success' || $transaction->status === 'paid' ? 'success' : 'warning' }}">{{ $transaction->status }}</span></td><td>{{ $transaction->created_at->format('d/m/Y H:i') }}</td></tr>@empty<tr><td colspan="7" class="text-center py-5 text-muted">Chưa có giao dịch.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $transactions->links() }}</div></div></div></body></html>
